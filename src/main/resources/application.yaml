ktor:
  application:
    modules:
      - com.github.radlance.ktormessagingapi.ApplicationKt.module
  deployment:
    port: 8080
postgres:
  url: "jdbc:postgresql://localhost:5432/messaging_api"
  user: postgres
  password: 1
jwt:
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
